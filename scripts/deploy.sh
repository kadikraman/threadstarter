echo "Deploying!"
echo this is a variable - ${TEST}

# uncomment v to run the deploy script
# ./node_modules/.bin/sls config credentials --provider aws --key ${AWS_KEY} --secret ${AWS_SECRET}
# ./node_modules/.bin/sls deploy -v
